First need to install Google Chrome

second need to install Chrome Driver:
  https://chromedriver.chromium.org/downloads
    ==> Need to select the same version of the Google Chrome Browser.
    ==> Need to select windows version.
    ==> After downlaoding need to extract the file.
    main.py:
    =======
      Need to setup 
      chrome_driver_path = chrome_driver_path = "S:\Devops\Downloading files(temp)\chrome_driver_folder\chromedriver.exe"
 ===========
selenium installation:
===========
import selenium
